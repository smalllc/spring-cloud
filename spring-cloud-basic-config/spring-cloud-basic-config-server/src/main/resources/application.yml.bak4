#找路径
server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/smalllc/spring-cloud-learning
          search-paths:
            - foo #路径
            - bar #路径

