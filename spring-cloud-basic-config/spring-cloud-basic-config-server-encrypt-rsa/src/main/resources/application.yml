#用户名密码
server:
  port: 8080
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/smalllc/spring-cloud-learning
          username: xxxx
          password: xxxx
encrypt:
  keyStore:
    location: classpath:/server.jks
    password: letmein
    alias: mytestkey
    secret: changeme