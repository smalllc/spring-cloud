spring:
  cloud:
    config:
      profile: dev
      label: master
      uri: http://localhost:8080    # 当ConfigServer的后端存储是Git时，默认就是master
    bus:
      trace:
        enabled: ture
  application:
    name: foobar
